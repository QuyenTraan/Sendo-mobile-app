import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'admin-hero-detail',
    templateUrl: './admin-hero-detail.component.html'
})
export class AdminHeroDetailComponent {
    private _hero;
}